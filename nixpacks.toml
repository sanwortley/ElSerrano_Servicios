# Instrucciones explicitas para Railway
[phases.setup]
nixPkgs = ["nodejs", "python3"]

[phases.install]
cmds = ["cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "cd frontend && npm run dev -- --host 0.0.0.0 --port $PORT"
