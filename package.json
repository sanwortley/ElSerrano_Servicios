{
    "name": "el-serrano-servicios",
    "version": "1.0.0",
    "description": "Sistema de Gestión El Serrano",
    "main": "index.js",
    "scripts": {
        "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"yellow,cyan\" \"cd backend && ..\\.venv\\Scripts\\python.exe -m uvicorn src.app:app --reload\" \"npm run dev --prefix frontend\"",
        "install:all": "pip install -r requirements.txt && npm install --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
